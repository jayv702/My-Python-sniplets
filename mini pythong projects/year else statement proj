year=int(input("what year?"))

#this one was pretty difficult due to the nesting and the if and else statements confusing me a lot,
#
if year%4 == 0:
    if year%100==0:
        if year%400==0:
            print("leap year")
        else:
            print("not a leap year")
    else:
        print ("leap year")
else:
    print("not a leap year")

if year
